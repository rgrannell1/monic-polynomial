---
- name: solve equations (asyncronously)
  shell: "screen -S 'draw-solutions' -d -m {{ repo_path }}/src/python/solve-polynomials.py --order={{ args.solve.order }} --range={{ args.solve.range }} --assume-yes"
